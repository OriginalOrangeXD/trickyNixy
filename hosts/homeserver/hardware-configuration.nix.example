# Replace this file with the real one generated on the target box:
#
#   ssh robby@192.168.1.254
#   sudo nixos-generate-config --show-hardware-config > /tmp/hardware.nix
#   exit
#   scp robby@192.168.1.254:/tmp/hardware.nix \
#       ~/git/naked/turnstone/nix/hosts/homeserver/hardware-configuration.nix
#
# Then uncomment the `./hardware-configuration.nix` import in
# hosts/homeserver/configuration.nix.
{ ... }: {
  # placeholder — overwritten by the generated file
}
